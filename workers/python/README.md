# fintuning-workers

Workers Python pour orchestrer les jobs de fine-tuning et la synchronisation Hugging Face.

## Environnement
- Python 3.11+
- Poetry 1.7+

```
poetry install --with dev
poetry run fintuning-worker --help
```
